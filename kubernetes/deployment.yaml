apiVersion: apps/v1
kind: Deployment
metadata:
  name: nightcrow-backend
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: backend
        image: nightcrow/backend:v2.6